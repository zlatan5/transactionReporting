version: '3.8'

services:
  pismo-transaction-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pismo-transaction-service
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v1/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - pismo-network

  # Optional: Add a proper database for production use
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: pismo-postgres
  #   environment:
  #     POSTGRES_DB: pismo_db
  #     POSTGRES_USER: pismo_user
  #     POSTGRES_PASSWORD: pismo_pass
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - pismo-network

networks:
  pismo-network:
    driver: bridge

# volumes:
#   postgres_data:
